# API Contracts

## Purpose

This directory contains the approved backend API contracts.

API contracts are part of the project architecture.

Implementation must follow these contracts.

If an implementation requires changing a contract:

STOP.

Update the architecture.

Approve the change.

Then continue implementation.

---

Each module owns its own API document.

This keeps contracts small, maintainable, and versionable.
